pre-commit:
  parallel: true
  commands:
    "lint:apply":
      glob: "*.{js,ts,cjs,mjs,d.cts,d.mts,jsx,tsx,json,jsonc,yml,yaml}"
      run: npm run lint:apply

    "typecheck":
      glob: "*.{ts,tsx}"
      run: npx tsc --noEmit

commit-msg:
  scripts:
    "commitlint.sh":
      runner: bash